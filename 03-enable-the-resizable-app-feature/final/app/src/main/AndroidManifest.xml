<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.raywenderlich.android.walloftext">

  <application
    android:name=".App"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:resizeableActivity="true"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.WallOfText">
    <activity
      android:name=".ui.notes.NotesActivity"
      android:configChanges="screenSize|screenLayout|smallestScreenSize|orientation"
      android:resizeableActivity="true">

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name=".ui.addNote.AddNoteActivity"
      android:configChanges="screenSize|screenLayout|smallestScreenSize|orientation"
      android:windowSoftInputMode="adjustPan|adjustResize" />
  </application>

</manifest>